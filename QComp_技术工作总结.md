# QComp 技术工作总结

## 第一部分：实验分析总结

我看了你的技术报告《QComp 技术报告：AWQ
量化误差的激活空间低秩补偿》。简单总结的话，你做的不是一个普通的"调参实验"，而是一个比较完整的
**LLM 量化误差补偿系统设计实验**。

一句话概括：

> 你提出并验证了一个不修改 AWQ 4bit 权重，通过激活空间低秩 adapter
> 补偿量化误差的方法，在 Qwen2.5-0.5B 上把 AWQ 的 PPL 降低
> 2.32%，同时保持 92.7% 推理吞吐。

------------------------------------------------------------------------

# 1. 这个实验主要做了哪些工作？

## 工作1：发现 AWQ 量化带来的误差问题

你首先分析：

FP16：

Y_fp16 = X @ W_fp16\^T

AWQ：

Y_awq = X @ W_awq\^T

二者存在：

DeltaY = Y_fp16 - Y_awq

该误差逐层累积，导致 PPL 上升。

------------------------------------------------------------------------

## 工作2：提出 QComp 方法

核心思想：

不修改量化权重 W_awq，而是在输出端增加补偿：

DeltaY ≈ (X B\^T) A\^T

其中：

-   A：\[out, rank\]
-   B：\[rank, in\]

rank 较小，例如 rank=8。

推理时：

output = AWQ_output + alpha \* linear(linear(x,B),A)

增加两个轻量 GEMM。

------------------------------------------------------------------------

## 工作3：设计 adapter 拟合算法

首先计算：

T = Y_fp - Y_awq

然后通过 Ridge Regression：

W_eff^T=(X^T X+lambda I)\^(-1) X\^T T

获得有效误差矩阵。

随后进行激活加权 SVD：

W_eff -\> A,B

形成低秩补偿 adapter。

------------------------------------------------------------------------

## 工作4：构建完整工程流程

实现：

-   FP16/AWQ误差采集
-   layer scoring
-   Ridge拟合
-   SVD分解
-   Adapter生成
-   AWQ运行时注入
-   PPL评估
-   性能benchmark

形成完整闭环。

------------------------------------------------------------------------

## 工作5：完成系统性实验验证

测试：

-   rank影响
-   lambda影响
-   alpha搜索
-   单层补偿
-   多层补偿

最终：

top40 + rank8 + lambda=0.1 + alpha=0.35

取得最佳效果。

------------------------------------------------------------------------

# 2. 实验亮点

## 亮点1：问题定义清晰

约束：

-   不修改qweight
-   不生成dense correction matrix
-   不破坏AWQ推理结构

更接近工业部署需求。

------------------------------------------------------------------------

## 亮点2：从weight-space转向output-space

失败实验发现：

直接优化：

W_fp16-Q_awq

效果不好。

原因：

模型最终感知的是：

XW

而不是：

W。

因此改为：

T=Y_fp-Y_awq

更加符合真实误差传播过程。

------------------------------------------------------------------------

## 亮点3：发现PPL优化比输出误差优化更有效

传统方法优化：

\|\|T-alpha\*XBA\|\|

但你发现：

输出误差最优alpha不等于语言模型效果最优alpha。

因此采用：

PPL-based alpha search。

------------------------------------------------------------------------

## 亮点4：兼顾精度和性能

结果：

-   PPL：21.5365 -\> 21.0379
-   提升：2.32%
-   throughput保持率：92.7%

------------------------------------------------------------------------

## 亮点5：失败实验分析充分

记录：

-   Weight-space失败
-   lstsq过拟合
-   calibration mismatch
-   alpha选择错误

体现了完整研究过程。

------------------------------------------------------------------------

# 3. 不足

## 1. 模型规模较小

目前验证：

Qwen2.5-0.5B

需要扩展：

-   7B
-   14B
-   70B

------------------------------------------------------------------------

## 2. 数据规模较小

当前：

100 texts

18863 tokens

需要更大benchmark。

------------------------------------------------------------------------

## 3. 缺少baseline比较

需要加入：

-   GPTQ
-   SmoothQuant
-   OmniQuant
-   QLoRA recovery

------------------------------------------------------------------------

## 4. Kernel优化不足

目前：

PyTorch GEMM

未来需要：

CUDA/Triton融合。

------------------------------------------------------------------------

# 第二部分：项目汇报风格总结

# 三、LLM量化误差低秩补偿技术研究

面向大模型低比特量化推理过程中精度下降问题，研究AWQ
W4A16量化模型的误差恢复方法，提出基于激活空间低秩补偿的QComp技术。通过分析量化前后模型输出差异，建立输出误差低秩近似模型，在不修改量化权重、不破坏量化推理结构的前提下，引入轻量级Adapter实现量化误差补偿，完成算法设计、训练拟合、推理集成及性能验证闭环。

------------------------------------------------------------------------

# 成果与亮点

1.  提出QComp激活空间低秩补偿方法，将量化输出误差建模为：

DeltaY ≈ (XB^T)A^T

通过两个轻量GEMM实现误差恢复，避免生成稠密校正矩阵，保持AWQ量化权重格式不变。

2.  完成完整量化补偿系统开发，实现FP16/AWQ双模型误差采集、Ridge回归拟合、激活加权SVD分解、Adapter生成及运行时注入流程，形成端到端验证链路。

3.  针对传统误差优化指标与语言模型效果不一致问题，引入基于PPL的Alpha搜索策略，直接优化模型语言建模能力；在Qwen2.5-0.5B模型上，AWQ基础PPL由21.5365降低至21.0379，提升2.32%。

4.  完成多层补偿策略研究，通过72层误差敏感度分析选择高收益层进行补偿，在40层补偿场景下保持92.7%吞吐率，实现精度与性能平衡。

------------------------------------------------------------------------

# 困难与应对

1.  针对初期权重空间误差补偿效果不佳的问题，将优化目标由Weight-Space转向Output-Space，采用：

T=Y_fp-Y_awq

作为补偿目标。

2.  针对小规模校准数据导致过拟合问题，引入Ridge正则化，并优化校准数据分布。

3.  针对传统输出重构误差无法代表语言模型效果的问题，研究PPL驱动参数搜索方法。

4.  针对补偿引入额外计算开销问题，采用低秩Adapter设计，仅增加rank=8的小规模矩阵计算。

------------------------------------------------------------------------

# 反思

当前QComp方法已验证低秩补偿在小规模模型上的有效性，但仍存在进一步优化空间：

1.  需要扩展至7B及以上规模模型验证泛化能力。

2.  当前采用全局Alpha控制，需要研究Layer-wise自适应补偿。

3.  当前运行时采用通用GEMM实现，需要结合CUDA/Triton
    Kernel融合优化，实现工业级部署。
